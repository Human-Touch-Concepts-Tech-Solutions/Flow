
services:
  backend:
    build: ./backend
    container_name: backend_service
    ports:
      - "8000:8000"
    # --- KEY ADDITIONS FOR HOT-RELOAD ---
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app  # Maps your local folder to the container folder
    environment:
      - PYTHONPATH=/app # Tells Python that 'app' is the root package
    # ------------------------------------
    deploy:
      resources:
        limits:
          memory: 4g

 